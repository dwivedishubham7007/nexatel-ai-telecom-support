# Add these to your existing backend/.env
CRM_MODE=internal
CRM_WEBHOOK_URL=
CRM_API_KEY=

# Browser prototype needs no paid ASR/TTS key.
# Production: telephony audio -> ASR -> /voice/chat -> TTS -> caller
